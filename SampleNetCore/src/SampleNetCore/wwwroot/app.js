var React = require('react');

var App = React.createClass({
    render(){

        return (

			    <div>
				Hello from React.
                </div>

            );
    }
});

module.exports = App;